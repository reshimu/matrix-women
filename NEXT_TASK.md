# Next atomic task

**Task:** Define the renderer lifecycle contract and implement a browser-only CSS renderer host that pauses on hidden/offscreen states and cleans up deterministically.

**Done when:** the lifecycle interface has explicit start/pause/resume/dispose behavior; browser APIs are isolated from the public library entry; unit tests cover visibility and intersection state transitions; and the project records have evidence.

**Current blocker:** no technical blocker. Git history is unavailable until a repository is initialized.